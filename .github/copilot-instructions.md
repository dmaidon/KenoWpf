# Copilot Instructions

## Project Guidelines
- Always run `Get-Date -Format "yyyy-MM-dd"` in the terminal before writing any "Last Edit:" header in KenoWpf repo files to ensure the date is always correct. Never guess or infer the date from context.